import React, { FC } from 'react';

const MainContent: FC = () => {
	return (
		<main className="container mt-4 md:flex  flex-row-reverse justify-evenly items-center bg-blue-500">
			Main Content
		</main>
	);
};

export default MainContent;
